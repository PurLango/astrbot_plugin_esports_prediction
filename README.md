# 赛事积分竞猜

面向 AstrBot 群聊的电竞赛事胜者竞猜插件。机器人自动收集赛程与赛果，群成员使用积分竞猜，比赛结束后自动结算。

- 当前版本：`2.5.1`
- 最低 AstrBot 版本：`4.22.0`
- 数据来源：[PandaScore](https://pandascore.co/)

## 主要功能

- 自动同步赛程、比赛状态与最终结果
- 群聊赛事列表和竞猜结果优先显示 PandaScore 提供的官方战队缩写
- 支持比赛胜者竞猜
- 根据历史赛果运行 Elo 模型，生成双方胜率与竞猜倍率
- 第一笔下注后锁定该场倍率，避免比赛过程中发生变化
- 同一用户在不同群对同一比赛的下注会合并为一笔全局记录
- 支持同队追加、改选和撤单
- 比赛结束后自动结算；取消、延期等异常比赛统一全额退款
- 展示盈利榜、命中率榜和总返还榜
- 在原积分运营台内提供赛事竞猜管理页签

## 竞猜规则

- 每次下注最低 `10` 积分，每人每场累计最高 `10,000` 积分
- 开赛前 `30` 分钟封盘，封盘后不能新下注或继续追加
- 开赛前 `60` 分钟截止改选与撤单
- 同一队伍可以多次追加，累计金额不得超过单场上限
- 猜中返还：`下注积分 × 锁定倍率`，结果向下取整
- 未猜中不返还本金；异常比赛原额退款
- 比赛、下注和排行榜均为 Bot 全局数据，不按群重复建立

倍率由插件根据已同步历史赛果的 Elo 实力评分计算，不读取博彩盘口。PandaScore 负责提供赛程、状态和赛果。

## 安装

1. 下载或克隆本仓库。
2. 将插件放入 AstrBot 的 `plugins` 目录。
3. 为兼容原积分数据，建议插件目录保持为 `astrbot_plugin_point_system`。
4. 重启 AstrBot 或在插件管理中重新加载。
5. 在插件配置中填写 PandaScore Token，并配置积分管理员账号。

插件本身没有额外的第三方 Python 依赖，运行环境由 AstrBot 提供。

## 首次配置

在 AstrBot 插件配置中找到 `esports_prediction_settings`：

- `enabled`：启用赛事竞猜
- `pandascore_token`：PandaScore API Token
- `games`：同步 `lol`、`valorant` 中的哪些项目
- `sync_enabled`：启用定时自动同步
- `sync_interval_minutes`：同步间隔，默认 `10` 分钟
- `min_bet` / `max_bet`：单次最低与每场累计最高下注
- `switch_deadline_minutes`：改选和撤单截止时间
- `close_before_minutes`：封盘时间
- `elo_k_factor`：Elo 评分更新速度
- `odds_margin`：模型倍率边际
- `timezone_offset_hours`：显示和手动录入赛事所用时区，默认 `UTC+8`

PandaScore Token 只应保存在 AstrBot 本地配置中，不要发送到群聊或提交到仓库。未配置 Token 时，管理员仍可手动添加、结算和退款。

积分管理员由 `admin_settings.points_admin_ids` 配置。个人 QQ 填写 QQ 号，QQ 官方机器人填写消息账号 OpenID。测试时可先使用 `/给积分 @用户 1000` 发放积分。

## 指令一览

### 日常积分

| 指令 | 功能简介 |
| --- | --- |
| `/签到` | 领取每日签到积分 |
| `/我的积分` | 查看积分余额、连续签到和今日签到状态 |
| `/积分记录` | 查看个人最近五次积分增减、来源和变动后余额；也可使用 `/积分明细` |
| `/积分榜` | 查看本群积分排行；本群暂无记录时展示总榜 |
| `/积分规则` | 查看当前签到、活跃、抽奖和兑换规则 |
| `/抽奖 [个人/群体]` | 使用积分参加个人或群体抽奖；不填写时采用默认模式 |
| `/记录生日 MM/DD` | 记录自己的生日，例如 `/记录生日 10/24` |
| `/生日签到` | 在生日当天领取生日奖励 |
| `/抢红包 [编号] [口令]` | 领取当前群的积分红包；也可使用 `/领红包` 或 `/红包` |

### 赛事竞猜

| 指令 | 功能简介 |
| --- | --- |
| `/赛事竞猜` | 查看竞猜功能的使用方法 |
| `/今日赛事 [撸/瓦]` | 查看今日及近期赛事；填写“撸”或“瓦”可只看对应游戏，不填写时均衡展示两边 |
| `/竞猜 编号 战队缩写 积分` | 使用积分竞猜胜者；支持战队缩写、`1`、`2` 或全名 |
| `/改选 编号 战队缩写` | 在截止时间前更换所选队伍 |
| `/撤销竞猜 编号` | 在截止时间前撤单并退回本金；也可使用 `/撤单` |
| `/我的竞猜` | 查看个人竞猜记录；也可使用 `/竞猜记录` |
| `/比赛结果` | 查看最近已结束比赛的比分和竞猜赛果；也可使用 `/赛事结果` 或 `/竞猜结果` |
| `/竞猜排行` | 查看盈利、命中率和总返还排行 |
| `/竞猜规则` | 查看当前竞猜规则和时间限制 |

### 积分兑换

| 指令 | 功能简介 |
| --- | --- |
| `/兑换列表` | 查看已上架兑换物、所需积分和库存 |
| `/兑换 兑换物名称` | 使用积分兑换指定物品 |
| `/兑换头衔 头衔内容` | 使用积分兑换自己的群头衔 |
| `/兑换设精` | 回复一条消息后，使用积分将其设为群精华 |
| `/兑换禁言 [@用户]` | 使用积分兑换禁言；默认对自己生效，是否可指定他人由配置决定 |

示例：

```text
/赛事竞猜
/今日赛事
/竞猜 L001 TES 100
/改选 L001 BLG
/撤销竞猜 L001
```

### 管理员指令

| 指令 | 功能简介 |
| --- | --- |
| `/竞猜管理 同步` | 立即同步赛程、赛果并执行自动结算 |
| `/竞猜管理 添加 游戏\|赛事\|队伍A\|队伍B\|时间` | 手动添加 LoL 或 VALORANT 比赛 |
| `/竞猜管理 结算 编号 队伍` | 手动指定获胜队伍并结算竞猜 |
| `/竞猜管理 退款 编号` | 全额退回该场所有待结算下注 |
| `/竞猜管理 封盘 编号` | 立即停止下注 |
| `/竞猜管理 隐藏 编号` | 从用户赛事列表隐藏比赛 |
| `/竞猜管理 显示 编号` | 重新显示比赛 |
| `/给积分 @用户 数量` | 为指定用户增加积分，也支持填写 QQ 号 |
| `/扣积分 @用户 数量` | 扣除指定用户的积分，也支持填写 QQ 号 |
| `/积分红包 固定 每份积分 份数` | 创建固定金额红包；也可使用 `/发红包` |
| `/积分红包 拼手气 总积分 份数` | 创建随机金额红包 |
| `/积分红包 口令 总积分 份数 口令` | 创建需要口令才能领取的红包 |
| `/清空所有数据 确认` | 清空积分、竞猜、抽奖、红包、生日和群记录；兑换记录保留 |

手动添加示例：

```text
/竞猜管理 添加 lol|LPL|BLG|TES|2026-09-01 19:00
```

## 积分运营台

入口：`AstrBot 管理面板 → 插件 → 赛事积分竞猜 → 积分运营台`。

管理页面可以：

- 配置 PandaScore Token、同步开关和游戏范围
- 编辑个人抽奖奖项、奖励区间和权重，并预览概率与期望返还
- 查看比赛状态与竞猜开放状态，并按游戏筛选
- 手动同步或添加比赛
- 封盘、隐藏、显示、退款和结算

## 数据与兼容

- 插件标识继续使用 `astrbot_plugin_point_system`，用于兼容原积分账户和已有数据
- 主数据文件：`<AstrBot数据目录>\plugin_data\astrbot_plugin_point_system\points_data.json`
- 竞猜数据与积分数据一起原子保存，并沿用原插件的自动备份能力
- 旧数据加载后会自动迁移到当前数据版本

## 项目说明

本项目基于 [menglimi/astrbot_plugin_point_system](https://github.com/menglimi/astrbot_plugin_point_system) 扩展开发，原插件提供了积分账户和持久化基础。
